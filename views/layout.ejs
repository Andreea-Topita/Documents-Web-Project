<!DOCTYPE html>
<html lang="ro">

    <head>
        <meta charset="UTF-8">
        <title>PW</title>
        <link rel="stylesheet" href="/css/style.css">
        
    </head>

    <body>
    <header>
    <h1>Documente</h1>
        <% if (user) { %>
            <div class="nav-left">
                <span>Autentificat ca <strong><%= user.nume %> <%= user.prenume %></strong></span>
                <a href="/deconectare" class="logout">🚪 Deconectare</a>

            </div>

            <div class="nav-right">
                <a href="/vizualizare-cos" class="cart-link">🛒 Coș</a>
                 <% if (userType === 'ADMIN') { %>
                    <a href="/admin" class="cart-link">⚙️ Admin</a>
                <% } %>
            </div>
        <% } %>
    </header>

        <main>
            <%-body %>
            
        </main>
        <hr>
        <footer>
            <p>&copy; 2025 Topiță Andreea</p>
        </footer>

    </body>
</html>